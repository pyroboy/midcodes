{
    "framework": "sveltekit",
    "outputDirectory": ".svelte-kit/output",
    "regions": ["sin1"],
    "version": 2,
    "installCommand": "pnpm install --no-frozen-lockfile",
    "buildCommand": "cd ../.. && turbo run build --filter=./apps/travel...",
    "ignoreCommand": "git diff HEAD^ HEAD --quiet ."
}