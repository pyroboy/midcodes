// src/lib/test-utils/environment.ts
export const configureTestEnv = () => {
  // Configure test environment for snapshots
  // This can be expanded later with more configuration as needed
  return {
    setup: () => {
      // Add any necessary setup
    },
    teardown: () => {
      // Add any necessary teardown
    }
  };
};
